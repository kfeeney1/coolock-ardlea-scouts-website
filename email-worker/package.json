{
  "name": "coolock-ardlea-scouts-email-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --config wrangler.production.toml"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
