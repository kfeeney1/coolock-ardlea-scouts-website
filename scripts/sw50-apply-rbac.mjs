import fs from 'node:fs';

function patch(path, replacements) {
  let text = fs.readFileSync(path, 'utf8');
  for (const [from, to] of replacements) {
    if (!text.includes(from)) throw new Error(`Missing expected text in ${path}: ${from.slice(0,120)}`);
    text = text.replace(from, to);
  }
  fs.writeFileSync(path, text);
}

patch('src/components/admin/LeaderDashboardHeader.tsx', [
  ['type NavItem = { label: string; path: string; adminOnly?: boolean; activityLogOnly?: boolean; settingsOnly?: boolean; };', 'type NavItem = { label: string; path: string; adminOnly?: boolean; leaderAccessOnly?: boolean; activityLogOnly?: boolean; settingsOnly?: boolean; };'],
  ['{ label: "Leader Access", path: "/leader/access", adminOnly: true },', '{ label: "Leader Access", path: "/leader/access", leaderAccessOnly: true },'],
  [' const canViewSettings = isAdmin || isGroupLeadership || adminProfile?.scoutingRole === "Group Treasurer";\n const canView = (item: NavItem) => (!item.adminOnly || isAdmin) && (!item.activityLogOnly || canViewActivityLog) && (!item.settingsOnly || canViewSettings);', ' const canViewSettings = isAdmin || isGroupLeadership || adminProfile?.scoutingRole === "Group Treasurer";\n const canViewLeaderAccess = isAdmin || isGroupLeadership;\n const canView = (item: NavItem) => (!item.adminOnly || isAdmin) && (!item.leaderAccessOnly || canViewLeaderAccess) && (!item.activityLogOnly || canViewActivityLog) && (!item.settingsOnly || canViewSettings);']
]);

patch('src/pages/LeaderAccessManagement.tsx', [
  ['import { recordAuditEvent } from "../services/auditLog";\n', 'import { CANONICAL_SCOUTING_APPOINTMENTS } from "../security/scoutingAppointments";\nimport { appointmentsActorMayAssign, canChangeSystemRole, canManageSectionScope, canOpenLeaderAccess } from "../security/leaderDelegationPolicy";\n'],
  ['  if (!adminProfile || !["admin", "super-admin"].includes(adminProfile.role)) {\n    return <Container maxWidth="xl" sx={{ py: { xs: 4, md: 6 } }}><Alert severity="error">Administrator access is required.</Alert></Container>;\n  }', '  const actor = adminProfile ? { uid: adminProfile.uid, systemRole: adminProfile.role, scoutingAppointment: adminProfile.scoutingRole } : null;\n  if (!actor || !canOpenLeaderAccess(actor)) {\n    return <Container maxWidth="xl" sx={{ py: { xs: 4, md: 6 } }}><Alert severity="error">Group Leadership or Administrator access is required.</Alert></Container>;\n  }\n  const isAdminActor = adminProfile.role === "admin" || adminProfile.role === "super-admin";'],
  ['      await updateLeaderAccess(record, user.uid);\n      await recordAuditEvent({ category: "leader-access", action: "Leader access and organisation updated", targetId: record.uid, targetLabel: record.displayName || record.email, section: record.organisationSection, description: `Saved system role ${record.role}; scouting role ${record.scoutingRole || "Leader"}; organisation section ${record.organisationSection}; public listing ${record.showPublicly ? "enabled" : "disabled"}.` });', '      await updateLeaderAccess(record, user.uid, user.email || adminProfile.email || "");'],
  ['    <LeaderPageHeader title="Leader Access & Organisation" description="Manage account access and each leader\'s place in the scouting hierarchy. Only a Super Admin can grant or remove Admin access. Public Who\'s Who publication is opt-in." actions={<Button variant="outlined" color="secondary" onClick={() => void refresh()}>Refresh</Button>} />', '    <LeaderPageHeader title="Leader Access & Organisation" description="Manage permitted leader assignments. System access roles remain Super Admin-only; Group Leadership can delegate ordinary operational appointments and section scope only." actions={<Button variant="outlined" color="secondary" onClick={() => void refresh()}>Refresh</Button>} />'],
  ['          <Select size="small" value={record.role} disabled={adminProfile.role !== "super-admin" || record.role === "super-admin"} onChange={(e) => patch(record.uid, { role: e.target.value as SystemRole })} sx={{ minWidth: 180 }}>', '          <Select size="small" value={record.role} disabled={!canChangeSystemRole(actor, { uid: record.uid, systemRole: record.role, scoutingAppointment: record.scoutingRole })} onChange={(e) => patch(record.uid, { role: e.target.value as SystemRole })} sx={{ minWidth: 180 }}>'],
  ['          <FormControlLabel control={<Switch checked={record.active} disabled={record.role === "super-admin"} onChange={(e) => patch(record.uid, { active: e.target.checked })} />} label="Active" />', '          <FormControlLabel control={<Switch checked={record.active} disabled={!isAdminActor || record.role === "super-admin"} onChange={(e) => patch(record.uid, { active: e.target.checked })} />} label="Active" />'],
  ['onClick={() => toggleSection(record, section)}>{section}</SectionToggleButton>', 'disabled={!canManageSectionScope(actor, { uid: record.uid, systemRole: record.role, scoutingAppointment: record.scoutingRole })} onClick={() => toggleSection(record, section)}>{section}</SectionToggleButton>'],
  ['          <TextField label="Scouting role / title" value={record.scoutingRole} onChange={(e) => patch(record.uid, { scoutingRole: e.target.value })} placeholder="e.g. Cub Section Leader" />', '          <TextField select label="Scouting appointment" value={record.scoutingRole} disabled={record.role !== "leader" || record.uid === actor.uid} onChange={(e) => patch(record.uid, { scoutingRole: e.target.value })}><MenuItem value="">No appointment</MenuItem>{CANONICAL_SCOUTING_APPOINTMENTS.filter((appointment) => appointmentsActorMayAssign(actor).includes(appointment) || appointment === record.scoutingRole).map((appointment) => <MenuItem key={appointment} value={appointment}>{appointment}</MenuItem>)}</TextField>'],
  ['          <SectionSelect id={`organisation-section-${record.uid}`} label="Organisation section" value={record.organisationSection} options={sections} onChange={(e) => patch(record.uid, { organisationSection: e.target.value })} />', '          <SectionSelect id={`organisation-section-${record.uid}`} label="Organisation section" value={record.organisationSection} options={sections} disabled={!isAdminActor} onChange={(e) => patch(record.uid, { organisationSection: e.target.value })} />'],
  ['          <TextField label="Display order" type="number" value={record.organisationOrder}', '          <TextField disabled={!isAdminActor} label="Display order" type="number" value={record.organisationOrder}'],
  ['          <TextField select label="Reports to" value={record.reportsToUid}', '          <TextField select disabled={!isAdminActor} label="Reports to" value={record.reportsToUid}'],
  ['          control={<Switch checked={record.showPublicly} onChange={(e) => patch(record.uid, { showPublicly: e.target.checked })} />}', '          control={<Switch checked={record.showPublicly} disabled={!isAdminActor || record.role !== "leader"} onChange={(e) => patch(record.uid, { showPublicly: e.target.checked })} />}']
]);

patch('firestore.rules', [
  ['    function canManageSubsPolicy() { return isAdmin() || isGroupLeader() || isGroupTreasurer(); }', '    function canManageSubsPolicy() { return isAdmin() || isGroupLeader() || isGroupTreasurer(); }\n    function isOrdinaryLeaderTarget(userId) {\n      return exists(/databases/$(database)/documents/adminUsers/$(userId))\n        && get(/databases/$(database)/documents/adminUsers/$(userId)).data.role == "leader";\n    }\n    function isGroupLeadershipDelegableAppointment(value) {\n      return value in ["Group Chairperson", "Group Youth Champion", "Section Leader", "Assistant Section Leader", "Programme Scouter", "Scouter"];\n    }\n    function canDelegateLeaderTarget(userId) {\n      return isGroupLeader() && request.auth.uid != userId && isOrdinaryLeaderTarget(userId);\n    }'],
  ['        || (isAdmin() && resource.data.role == "leader"\n          && request.resource.data.role == "leader"', '        || ((isAdmin() || canDelegateLeaderTarget(userId)) && resource.data.role == "leader"\n          && request.resource.data.role == "leader"'],
  ['          && request.resource.data.diff(resource.data).affectedKeys().hasOnly(["active", "sections", "displayName", "mobileNumber", "updatedAt", "updatedBy"]))', '          && request.resource.data.updatedBy == request.auth.uid\n          && request.resource.data.updatedAt == request.time\n          && request.resource.data.diff(resource.data).affectedKeys().hasOnly(isAdmin() ? ["active", "sections", "displayName", "mobileNumber", "updatedAt", "updatedBy"] : ["sections", "updatedAt", "updatedBy"]))'],
  ['    match /organisationLeadership/{leaderId} {\n      allow read: if isActiveLeader();\n      allow create, update: if isAdmin()', '    match /organisationLeadership/{leaderId} {\n      allow read: if isActiveLeader();\n      allow create, update: if (isAdmin() || (canDelegateLeaderTarget(leaderId)\n          && request.resource.data.scoutingRole is string\n          && isGroupLeadershipDelegableAppointment(request.resource.data.scoutingRole)\n          && resource.data.diff(request.resource.data).affectedKeys().hasOnly(["scoutingRole", "updatedAt"])))'],
  ['      allow create, update: if isAdmin()\n        && request.resource.data.displayName is string\n        && request.resource.data.scoutingRole is string\n        && request.resource.data.organisationSection is string', '      allow create, update: if isAdmin()\n        && request.resource.data.displayName is string\n        && request.resource.data.scoutingRole is string\n        && request.resource.data.organisationSection is string'],
  ['      allow delete: if isAdmin();\n    }\n\n    match /publicLeadership/{leaderId}', '      allow delete: if isAdmin();\n    }\n\n    match /publicLeadership/{leaderId}']
]);

console.log('SW-50 guarded RBAC patches applied');
