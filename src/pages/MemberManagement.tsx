import LeaderDashboardHeader from "../components/admin/LeaderDashboardHeader";
import LeaderPageHeader from "../components/admin/LeaderPageHeader";
import FamilyRelationshipsPanel from "../components/admin/FamilyRelationshipsPanel";
import MemberStatusLifecycleDialog from "../components/admin/MemberStatusLifecycleDialog";
import BulkMemberSectionTransfer from "../components/admin/BulkMemberSectionTransfer";
import { OperationalEmptyState, OperationalErrorState, OperationalLoading, OperationalPermissionState } from "../components/admin/OperationalStates";
import { Alert, Box, Button, Chip, Container, Dialog, DialogActions, DialogContent, DialogTitle, FormControl, InputLabel, MenuItem, Paper, Select, Stack, TextField, Typography } from "@mui/material";
import { useCallback, useEffect, useMemo, useState } from "react";
import { Link, useSearchParams } from "react-router-dom";
import { createMember, loadMemberConsentSummaries, loadMembers, updateMember } from "../services/memberAdmin";
import type { CreateMemberInput, MemberConsentSummary, MemberRecord, MemberStatus } from "../services/memberAdmin";
import { classifyFirestoreFailure, firestoreFailureMessage } from "../services/firestoreErrors";
import { disableParentPortalAccess } from "../services/parentManagement";
import { parentLifecycleCandidates } from "../services/parentLifecycleLogic";
import type { ParentLifecycleCandidate } from "../services/parentLifecycleLogic";
import { loadParentAccounts } from "../services/parentPortal";
import { moveToUiTargetAfterRender } from "../services/uiTargeting";

const sections = ["all", "Beavers", "Cubs", "Scouts", "Ventures", "Rovers", "Group", "Other"];
const memberStatuses: MemberStatus[] = ["active", "inactive", "left"];
const emptyMember: CreateMemberInput = { firstName: "", lastName: "", displayName: "", dateOfBirth: "", section: "", parentName: "", emailAddress: "", mobileNumber: "", emergencyContactName: "", emergencyContactPhone: "", status: "active" };
const statusLabel = (status: MemberStatus) => status === "active" ? "Active" : status === "inactive" ? "Inactive" : "Left";
const statusColor = (status: MemberStatus): "success" | "warning" | "default" => status === "active" ? "success" : status === "inactive" ? "warning" : "default";
const formatDate = (value: Date | null) => value ? new Intl.DateTimeFormat("en-IE", { dateStyle: "medium", timeStyle: "short" }).format(value) : "Unknown";
const consentExpired = (value: string) => Boolean(value && value < new Date().toISOString().slice(0, 10));

export default function MemberManagement() {
  const [searchParams, setSearchParams] = useSearchParams();
  const initialStatus = searchParams.get("status");
  const [members, setMembers] = useState<MemberRecord[]>([]);
  const [loading, setLoading] = useState(true);
  const [memberLoadError, setMemberLoadError] = useState<unknown>(null);
  const [saveError, setSaveError] = useState("");
  const [message, setMessage] = useState("");
  const [search, setSearch] = useState(searchParams.get("q") || "");
  const [sectionFilter, setSectionFilter] = useState(searchParams.get("section") || "all");
  const [statusFilter, setStatusFilter] = useState<MemberStatus | "all">(initialStatus === "all" || memberStatuses.includes(initialStatus as MemberStatus) ? initialStatus as MemberStatus | "all" : "active");
  const [selected, setSelected] = useState<MemberRecord | null>(null);
  const [draft, setDraft] = useState<MemberRecord | null>(null);
  const [saving, setSaving] = useState(false);
  const [statusConfirmationOpen, setStatusConfirmationOpen] = useState(false);
  const [lifecycleCandidates, setLifecycleCandidates] = useState<ParentLifecycleCandidate[]>([]);
  const [consents, setConsents] = useState<MemberConsentSummary[]>([]);
  const [loadingConsents, setLoadingConsents] = useState(false);
  const [consentLoadError, setConsentLoadError] = useState<unknown>(null);
  const [addOpen, setAddOpen] = useState(false);
  const [addDraft, setAddDraft] = useState<CreateMemberInput>({ ...emptyMember });
  const [creating, setCreating] = useState(false);
  const [addError, setAddError] = useState("");

  const load = useCallback(async () => { setLoading(true); setMemberLoadError(null); try { setMembers(await loadMembers()); } catch (error) { console.error("Unable to load members:", error); setMemberLoadError(error); } finally { setLoading(false); } }, []);
  useEffect(() => { void load(); }, [load]);
  useEffect(() => {
    const next = new URLSearchParams();
    if (search.trim()) next.set("q", search);
    if (sectionFilter !== "all") next.set("section", sectionFilter);
    if (statusFilter !== "active") next.set("status", statusFilter);
    setSearchParams(next, { replace: true });
  }, [search, sectionFilter, statusFilter, setSearchParams]);
  const visibleMembers = useMemo(() => members.filter((member) => { if (sectionFilter !== "all" && member.section !== sectionFilter) return false; if (statusFilter !== "all" && member.status !== statusFilter) return false; const query = search.trim().toLowerCase(); return !query || [member.displayName, member.parentName, member.emailAddress, member.mobileNumber, member.section, member.emergencyContactName, member.emergencyContactPhone].join(" ").toLowerCase().includes(query); }), [members, sectionFilter, statusFilter, search]);
  const counts = useMemo(() => ({ total: members.length, active: members.filter((m) => m.status === "active").length, inactive: members.filter((m) => m.status === "inactive").length, left: members.filter((m) => m.status === "left").length }), [members]);
  const summary: Array<[string, number, MemberStatus | "all"]> = [["Total", counts.total, "all"], ["Active", counts.active, "active"], ["Inactive", counts.inactive, "inactive"], ["Left", counts.left, "left"]];
  const selectStatus = (status: MemberStatus | "all") => { setStatusFilter(status); moveToUiTargetAfterRender("member-results", { focus: true }); };
  const loadMemberConsents = async (member: MemberRecord) => { setLoadingConsents(true); setConsentLoadError(null); try { setConsents(await loadMemberConsentSummaries(member)); } catch (error) { console.error("Unable to load linked consents:", error); setConsentLoadError(error); } finally { setLoadingConsents(false); } };
  const closeMember = () => { setStatusConfirmationOpen(false); setLifecycleCandidates([]); setSelected(null); setDraft(null); };
  const openMember = async (member: MemberRecord) => { setSelected(member); setDraft({ ...member }); setConsents([]); setSaveError(""); setMessage(""); await loadMemberConsents(member); };
  const refreshSelectedMember = async () => { if (!selected) return; const refreshed = await loadMembers(); setMembers(refreshed); const next = refreshed.find((member) => member.id === selected.id) || null; if (next) { setSelected(next); setDraft({ ...next }); setMessage("Family relationship updated."); } else closeMember(); };
  const prepareLifecycleCandidates = async (memberId: string, nextStatus: MemberStatus) => { if (nextStatus === "active") return []; try { return parentLifecycleCandidates(await loadParentAccounts(), members, memberId, nextStatus); } catch { return []; } };
  const save = async (statusConfirmed = false, disableParents = false) => { if (!selected || !draft) return; if (!draft.displayName.trim()) return setSaveError("Member name is required."); if (!statusConfirmed && draft.status !== selected.status) { setLifecycleCandidates(await prepareLifecycleCandidates(selected.id, draft.status)); setStatusConfirmationOpen(true); return; } setSaving(true); setSaveError(""); setMessage(""); try { await updateMember(selected.id, { firstName: draft.firstName, lastName: draft.lastName, displayName: draft.displayName, dateOfBirth: draft.dateOfBirth, section: draft.section, parentName: draft.parentName, emailAddress: draft.emailAddress, mobileNumber: draft.mobileNumber, emergencyContactName: draft.emergencyContactName, emergencyContactPhone: draft.emergencyContactPhone, status: draft.status }); const updated = { ...draft, id: selected.id }; setMembers((current) => current.map((member) => member.id === selected.id ? updated : member)); setSelected(updated); setDraft(updated); if (disableParents && lifecycleCandidates.length > 0) await Promise.allSettled(lifecycleCandidates.map(({ parent }) => disableParentPortalAccess(parent, `Parent access disabled after ${updated.displayName} changed status and no other explicitly linked active child remained.`))); setMessage("Member details updated."); setLifecycleCandidates([]); } catch (error) { console.error("Unable to save member:", error); setSaveError("Unable to update the member record."); } finally { setSaving(false); } };
  const confirmStatusChange = (disableParents: boolean) => { setStatusConfirmationOpen(false); void save(true, disableParents); };
  const addMember = async () => { setAddError(""); if (!addDraft.displayName.trim()) return setAddError("Member name is required."); if (!addDraft.section) return setAddError("Select the member's section."); setCreating(true); try { await createMember(addDraft); setAddOpen(false); setAddDraft({ ...emptyMember }); await load(); } catch { setAddError("Unable to create the member record."); } finally { setCreating(false); } };
  const memberForm = (value: CreateMemberInput, setValue: (value: CreateMemberInput) => void) => <Box sx={{ display: "grid", gridTemplateColumns: { xs: "1fr", md: "1fr 1fr" }, gap: 2 }}><TextField label="First name" value={value.firstName} onChange={(e) => setValue({ ...value, firstName: e.target.value })} /><TextField label="Last name" value={value.lastName} onChange={(e) => setValue({ ...value, lastName: e.target.value })} /><TextField required label="Display name" value={value.displayName} onChange={(e) => setValue({ ...value, displayName: e.target.value })} /><TextField type="date" label="Date of birth" value={value.dateOfBirth} slotProps={{ inputLabel: { shrink: true } }} onChange={(e) => setValue({ ...value, dateOfBirth: e.target.value })} /><FormControl required><InputLabel>Section</InputLabel><Select label="Section" value={value.section} onChange={(e) => setValue({ ...value, section: e.target.value })}>{sections.filter((s) => s !== "all").map((s) => <MenuItem key={s} value={s}>{s}</MenuItem>)}</Select></FormControl><FormControl><InputLabel>Status</InputLabel><Select label="Status" value={value.status} onChange={(e) => setValue({ ...value, status: e.target.value as MemberStatus })}>{memberStatuses.map((s) => <MenuItem key={s} value={s}>{statusLabel(s)}</MenuItem>)}</Select></FormControl><TextField label="Parent / Guardian" value={value.parentName} onChange={(e) => setValue({ ...value, parentName: e.target.value })} /><TextField label="Email address" type="email" value={value.emailAddress} onChange={(e) => setValue({ ...value, emailAddress: e.target.value })} /><TextField label="Mobile number" value={value.mobileNumber} onChange={(e) => setValue({ ...value, mobileNumber: e.target.value })} /><TextField label="Emergency contact" value={value.emergencyContactName} onChange={(e) => setValue({ ...value, emergencyContactName: e.target.value })} /><TextField label="Emergency contact phone" value={value.emergencyContactPhone} onChange={(e) => setValue({ ...value, emergencyContactPhone: e.target.value })} /></Box>;
  const renderMemberResults = () => { if (loading) return <OperationalLoading minHeight={300} label="Loading member records" />; if (memberLoadError) { const failure = firestoreFailureMessage(memberLoadError, "Unable to load member records."); return classifyFirestoreFailure(memberLoadError) === "permission" ? <OperationalPermissionState title="Member records access restricted" actionLabel="Retry" onAction={() => void load()}>{failure}</OperationalPermissionState> : <OperationalErrorState title="Member records could not be loaded" actionLabel="Retry" onAction={() => void load()}>{failure}</OperationalErrorState>; } if (members.length === 0) return <OperationalEmptyState title="No member records">No member records are available for your assigned sections.</OperationalEmptyState>; if (visibleMembers.length === 0) return <OperationalEmptyState title="No matching members">No members match the current search and filters.</OperationalEmptyState>; return <Box sx={{ display: "grid", gap: 2 }}>{visibleMembers.map((member) => <Paper key={member.id} variant="outlined" sx={{ p: 2.5 }} data-testid={`member-card-${member.id}`}><Box sx={{ display: "flex", flexDirection: { xs: "column", md: "row" }, justifyContent: "space-between", gap: 2 }}><Box><Stack direction="row" spacing={1} useFlexGap sx={{ alignItems: "center", flexWrap: "wrap" }}><Typography variant="h5" color="secondary">{member.displayName}</Typography><Chip label={statusLabel(member.status)} color={statusColor(member.status)} size="small" /><Chip label={member.section} variant="outlined" size="small" />{member.familyId && <Chip label="Family linked" variant="outlined" size="small" color="info" />}</Stack><Typography sx={{ mt: 1 }}>Parent / Guardian: {member.parentName || "Not provided"}</Typography><Typography sx={{ mt: .5 }}>Phone: {member.mobileNumber || "Not provided"}</Typography></Box><Stack direction={{ xs: "column", sm: "row" }} spacing={1}><Button component={Link} to={`/leader/subs?member=${encodeURIComponent(member.id)}`} variant="outlined">Record subs</Button><Button variant="contained" color="success" onClick={() => void openMember(member)}>Manage</Button></Stack></Box></Paper>)}</Box>; };
  const consentIndicators = () => loadingConsents ? <OperationalLoading minHeight={120} label="Loading consent indicators" /> : consentLoadError ? <Alert severity="warning">Linked consent records could not be checked.</Alert> : consents.length === 0 ? <OperationalEmptyState title="No matching consent records">No matching consent records were found for this member.</OperationalEmptyState> : <Stack spacing={1}>{consents.map((consent) => <Paper key={consent.consentId} variant="outlined" sx={{ p: 2 }}><Stack direction="row" spacing={1}><Chip size="small" label={consentExpired(consent.consentTo) ? "Expired" : "Consent found"} color={consentExpired(consent.consentTo) ? "error" : "success"} />{consent.hasMedicalAlert && <Chip size="small" label="Medical alert" color="warning" />}{consent.hasMedicationManagement && <Chip size="small" label="Medication" color="error" />}</Stack><Typography variant="body2" sx={{ mt: 1 }}>{consent.section} · Submitted {formatDate(consent.submittedAt)}</Typography></Paper>)}</Stack>;
  return <Box sx={{ minHeight: "100vh", backgroundColor: "background.default", py: { xs: 4, md: 6 } }}><Container maxWidth="xl"><LeaderDashboardHeader /><LeaderPageHeader title="Member Management" description="Maintain member records, sections, family relationships, contacts and consent indicators." actions={<Stack direction={{ xs: "column", sm: "row" }} spacing={1.5}><Button variant="contained" color="secondary" onClick={() => { setAddDraft({ ...emptyMember }); setAddError(""); setAddOpen(true); }}>Add Member</Button><Button variant="contained" color="success" onClick={() => void load()}>Refresh</Button></Stack>} /><Box role="group" aria-label="Member status summary" sx={{ display: "grid", gridTemplateColumns: { xs: "1fr 1fr", md: "repeat(4, 1fr)" }, gap: 2, mb: 3 }}>{summary.map(([label, value, status]) => <Paper key={label} variant="outlined" role="button" tabIndex={0} aria-pressed={statusFilter === status} onClick={() => selectStatus(status)} onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); selectStatus(status); } }} sx={{ p: 2.5, textAlign: "center", cursor: "pointer", borderWidth: statusFilter === status ? 2 : 1 }}><Typography variant="h4" color="secondary">{value}</Typography><Typography variant="body2">{label}</Typography></Paper>)}</Box><Paper elevation={2} sx={{ p: 3, mb: 3 }}><Box sx={{ display: "grid", gridTemplateColumns: { xs: "1fr", md: "2fr 1fr 1fr" }, gap: 2 }}><TextField label="Search members" value={search} onChange={(e) => setSearch(e.target.value)} /><FormControl><InputLabel id="member-section-filter-label">Section</InputLabel><Select labelId="member-section-filter-label" label="Section" value={sectionFilter} onChange={(e) => setSectionFilter(e.target.value)}>{sections.map((s) => <MenuItem key={s} value={s}>{s === "all" ? "All Sections" : s}</MenuItem>)}</Select></FormControl><FormControl><InputLabel id="member-status-filter-label">Status</InputLabel><Select labelId="member-status-filter-label" label="Status" value={statusFilter} onChange={(e) => setStatusFilter(e.target.value as MemberStatus | "all")}><MenuItem value="all">All Statuses</MenuItem>{memberStatuses.map((s) => <MenuItem key={s} value={s}>{statusLabel(s)}</MenuItem>)}</Select></FormControl></Box></Paper>{!loading && !memberLoadError && members.length > 0 && <BulkMemberSectionTransfer members={members} visibleMembers={visibleMembers} onTransferred={load} />}<Box id="member-results" role="region" aria-label="Member results" tabIndex={-1}>{renderMemberResults()}</Box><Dialog open={Boolean(selected && draft)} onClose={closeMember} maxWidth="lg" fullWidth>{selected && draft && <><DialogTitle>Member — {draft.displayName}</DialogTitle><DialogContent dividers>{saveError && <Alert severity="error" sx={{ mb: 2 }}>{saveError}</Alert>}{message && <Alert severity="success" sx={{ mb: 2 }}>{message}</Alert>}<Typography variant="h5" color="secondary" sx={{ mb: 2 }}>Member Details</Typography>{memberForm(draft, (value) => setDraft({ ...draft, ...value }))}<Typography variant="h5" color="secondary" sx={{ mt: 4, mb: 2 }}>Family / Siblings</Typography><FamilyRelationshipsPanel member={selected} members={members} onChanged={refreshSelectedMember} /><Typography variant="h5" color="secondary" sx={{ mt: 4, mb: 2 }}>Consent Indicators</Typography>{consentIndicators()}</DialogContent><DialogActions><Button onClick={closeMember}>Close</Button><Button variant="contained" color="success" disabled={saving} onClick={() => void save()}>{saving ? "Saving..." : "Save Member"}</Button></DialogActions></>}</Dialog><MemberStatusLifecycleDialog open={Boolean(statusConfirmationOpen && selected && draft)} member={draft} previousStatus={selected?.status || null} candidates={lifecycleCandidates} saving={saving} onCancel={() => setStatusConfirmationOpen(false)} onMemberOnly={() => confirmStatusChange(false)} onMemberAndParents={() => confirmStatusChange(true)} /><Dialog open={addOpen} onClose={() => !creating && setAddOpen(false)} maxWidth="md" fullWidth><DialogTitle>Add Existing Member</DialogTitle><DialogContent dividers>{addError && <Alert severity="error" sx={{ mb: 3 }}>{addError}</Alert>}{memberForm(addDraft, setAddDraft)}</DialogContent><DialogActions><Button disabled={creating} onClick={() => setAddOpen(false)}>Cancel</Button><Button variant="contained" color="success" disabled={creating} onClick={() => void addMember()}>{creating ? "Adding..." : "Add Member"}</Button></DialogActions></Dialog></Container></Box>;
}
